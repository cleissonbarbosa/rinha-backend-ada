with "aws";

project Backend is
   for Main use ("main_backend", "main_lb");
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   package Compiler is
      for Default_Switches ("Ada") use ("-O2");
   end Compiler;
end Backend;
